# Ranger Configuration File

# Use neovim as the default editor
set default_editor nvim

# This determines which script is used to handle file opening
# rifle is the file opener used by ranger
set open_all_images true

# Show hidden files
set show_hidden true

# Preview files on the right side
set preview_files true
set preview_directories true

# Use one of the supported image preview protocols
set preview_images true

# Use the external preview script for detailed file previews
set use_preview_script true

# Automatically count files in the directory
set automatically_count_files true

# Show file size in the main column or in the status bar
set display_size_in_main_column true
set display_size_in_status_bar true

# Abbreviate $HOME with ~ in the titlebar (first line)
set tilde_in_titlebar true

# How many directory-changes or console-commands should be kept in history?
set max_history_size 20
set max_console_history_size 50

# Draw borders around columns
set draw_borders both

# Display the file size in the main column or status bar?
set display_size_in_main_column true

# Padding on the right when there's no preview
set padding_right true
